name: "Kinematics — wrong formula applied"
subject: physics

problem:
  document_name: "test_harness_physics"
  question_number: 1
  label: "Problem 1"
  text: "A car accelerates from rest at 3 m/s^2 for 8 seconds. How far does it travel?"
  parts: []
  answer_key:
    - part_label: null
      answer: "96 m (using d = (1/2)at^2)"

steps:
  - id: "start"
    transcription: "a = 3 m/s^2, t = 8s, v0 = 0"
    expect:
      action: silent

  - id: "wrong_formula"
    transcription: "a = 3 m/s^2, t = 8s, v0 = 0\nd = v * t\nd = 3 * 8 = 24 m"
    expect:
      action: speak
      constraints: [no_answer_reveal, graduated_intervention, brevity]
