"use client"

// SignupHero is no longer used — layout is now single-column centered.
// Keeping file to avoid import errors during transition.
export default function SignupHero() {
  return null
}
